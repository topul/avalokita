# 2026-08-15

## 观自在（avalokita）站点完成 & 神像精修

### 站点搭建
- 纯静态、零依赖：HTML+CSS+原生 JS，音效用 Web Audio 合成、图用内联 SVG
- 山门选择佛教/道门 → 殿堂 5 个仪轨：礼拜、上香、木鱼/云磬、念珠、功课
- 双主题切换：`body[data-tradition="buddha|tao|gate"]`，CSS 变量驱动
- 数据存 `localStorage`（key `guanzizai.v1`），按日重置 `cur`、累计 `total`
- Vercel 部署：`vercel.json` 纯静态配置（无构建、根目录输出、缓存头）
- 验证：jsdom 冒烟测试覆盖 5 模块核心交互，全部 PASS

### 神像库
- 两宗各 5 尊（佛门：释迦、观音、文殊、弥勒、财宝天王；道门：元始、灵宝、道德、玉皇、赵公明）
- 独立文件：`scripts/deities-buddha.js`、`scripts/deities-tao.js`，挂在 `window.DEITIES`
- 每尊 `{ id, title, short, honor, kind, sound, gatha, svg }`，svg 自包含 `<svg viewBox="0 0 600 560">`
- 渐变 id 用尊前缀（`sh_`/`gy_`/`ws_`/`ml_`/`cb_`/`yc_`/`lb_`/`dd_`/`yh_`/`zg_`）防冲突
- mod-bow 动态注入 + 按神累计（`Store.deityAdd/Count`）

### 神像精修（77f37aa）
- 用 `rsvg-convert` 把十尊渲染成 PNG，深色背景，肉眼核验构图
- 全面重画：法器放大、姿态准确、画出真手指、修正法器方位
  - 释迦：右手触地印明确按地、左手禅定托钵
  - 观音：净瓶含瓶口杨柳与明珠
  - 文殊：利剑含剑格剑首、经箧书页可见
  - 弥勒：双手抚长念珠搭身侧、布袋大而明显
  - 财宝天王：多层舍利塔、吐宝鼠衔元宝
  - 元始/灵宝：灵芝首玉如意
  - 道德：太极扇阴阳图、长白须、拂尘马尾
  - 玉皇：冕旒十二垂旒白玉珠、玉圭山形首
  - 赵公明：节段铁鞭、聚宝盆装金锭明珠、虬髯三绺
- 辅助：`handR` 通用圆胖手势函数，thumb+四指可辨
- 验证：XML 校验 10/10 合法；jsdom 选神测试 PASS

### 易错点（记住避免）
- `<radialGradient>` 内 `<stop>` 标签千万别漏写或重复 `stop-color`/`stop-opacity` 属性
- rsvg-convert 对 XML 属性重复严格报错，写完先跑 `/tmp/validate_svg2.py`
- jsdom 依赖被 `npm install xxx --no-save` 清掉时需重装

### 移动端体验增强（base.css，未改 JS）
- 全局 `touch-action:manipulation`（消 300ms 延迟+禁双击缩放）、`overscroll-behavior-y:contain`（防橡皮筋链到外层）
- 交互元素 `user-select:none` + `-webkit-touch-callout:none`（防长按选中/右键菜单，textarea/input 保留可编辑）；`svg{-webkit-user-drag:none}`
- 底部 rail 不再遮挡：≤899 `.panel{padding-bottom:calc(64px + safe-area-inset-bottom + 16px)}`，rail `z-index:5`；`.mat-ring/.mat-face{pointer-events:none}` 让长按落在按钮上
- 顶栏紧凑：≤640 压缩 gap/字号；≤560 `flex-wrap`（标题整行、工具换第二行）；≤420 隐藏功德 label
- 视口高度 `100vh;100dvh` 双写 fallback；`.mat` 已有 `touch-action:none` 防长按滚动
- 断点顺序注意：820/899/900+/1180/640/560/420，同特异性后者覆盖，door 的 flex-direction 由 820 控制未被覆盖
- 校验：CSS 括号 287/287 平衡；jsdom 选神冒烟全 PASS；HTTP 资源 200

### 参拜者细化（52f772a）
- 把礼拜面板里朝拜者从「头圆+袍团+小菱形手」重画为完整朝拜者
- 部件：光顶+顶髻圆球+束发根、闭目静穆面容、海青袍（中缝/衣纹/腰带）、胸前合十双手、脚下蒲团
- SVG 在 `index.html` `#devotee`，class 类：`.dv`（整体填充与 transform）/ `.dv-cushion` / `.dv-fold`（衣纹）/ `.dv-face`（面容） / `.dv-hands`（合十）
- 下俯弯腰动画仍走 CSS 变量 `--bend`，`.dv` 用 `translateY+scaleY+scaleX` 实现
- 调试技巧：把 inline SVG 抠出来 + 内联 `<style>` 用 rsvg-convert 渲染到 PNG 肉眼核验
- 校验：rsvg 渲染无 side-bump bug；CSS 290/290 平衡；HTTP 200